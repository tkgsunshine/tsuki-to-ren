# 月と蓮（Hasu-to-Tsuki）開発ルール・レギュレーション集約定義書（AGENTS.md）

本書は、本プロジェクト（『月と蓮』）に関わるすべてのAIエージェント（Antigravity, OpenAI Codex, Claude Code, Cursor, GitHub Copilot等）および開発者が遵守すべき共通ルール、安全性基準、コンテンツ制作レギュレーションを網羅・定義したものです。

---

## 1. 🛡️ 自動復元・コード安全性ルール（全AI共通最優先事項）

1. **自動 Git セーフティネットとロールバック**:
   - 作業対象の全プロジェクトにおいて、`.git` が存在しない場合は直ちに `git init` を実行すること。
   - 大きな機能追加や変更を行う前、および動作確認・ビルド成功後には `git commit` を行い、安全な作業ポイントを自動保存すること。
   - 万が一コード修正後に構文エラーやビルド失敗（`npm run build` / `npx tsc` 失敗）が発生した場合は、速やかに `git checkout .` または `git reset --hard` を実行し、1秒で正常な直前状態へ自動ロールバックすること。

2. **マルチバイト文字（日本語）および巨大ファイルのコード破損防止**:
   - 日本語の文字列リテラル（3バイト文字）やバッククォート（` `）、クォーテーション（`'`/`"`）を含むコードを部分置換する際は、文字切れや未閉じ文字列が発生しないよう厳重に注意すること。
   - 1,000行を超える巨大なファイルや設定データを編集する際は、部分置換による切れ目を避け、慎重かつ正確に全構造を維持して修正すること。

3. **作業終了前の必須ビルド検証**:
   - ファイルの編集完了後、必ず `npm run build`（型チェック `tsc -b`、バンドル、サイトマップ生成、静的HTML事前レンダリング）を実行してエラーが全件0であることを検証してから回答・作業完了とすること。

4. **推測・言い訳の全面禁止と厳格な事実報告**:
   - 質問やエラー、システム挙動の原因を回答する際、推測や適当な理由・言い訳を語ることを固く禁止する。
   - 必ず実際のコード、ログ、ターミナル実行結果という検証可能な「事実（ファクト）」のみを事前に裏付けて報告すること。
   - 不明な点や失敗が発生した場合は、誤魔化さず直ちにそのままの事実を簡潔かつ正確に報告すること。

5. **無断ファイル変更・削除の禁止**:
   - ユーザーから明示的な指示がない限り、プロジェクト内のファイルを勝手に削除したり、関連のないコードを改変しないこと。

---

## 2. 🔮 占星術・テキスト表現レギュレーション（高校生レベルの平易化）

ユーザー体験および読者層（10代後半〜20代）への配慮のため、難解な四柱推命・易経・MBTIの専門用語をユーザー向け表示テキストから完全に排除すること。

1. **禁止専門用語と平易な代替表現の対応表**:
   - ❌ `【比和】` ➔ ⭕ `【親友のように等身大でいられる相性】` / `【自然体】` / `【息ぴったり】`
   - ❌ `【支合】` ➔ ⭕ `【以心伝心】言葉にしなくても伝わる空気感` / `【最高のフィーリング】`
   - ❌ `【相生】` ➔ ⭕ `【自然と惹かれ合う相性】` / `【支え合い】` / `【運気アップ】`
   - ❌ `【相剋】` ➔ ⭕ `【お互いを刺激し合うドラマティック相性】` / `【刺激的】` / `【新しい刺激】`
   - ❌ `【干合】` ➔ ⭕ `【運命の相性】` / `【運命の絆】`
   - ❌ `【三合】` ➔ ⭕ `【相乗効果バツグン】一緒に未来へ進める相性` / `【ベストパートナー】`
   - ❌ `【六沖】` ➔ ⭕ `【真逆だから惹かれ合う】刺激的な相性` / `【磁石のような魅力】`
   - ❌ `【心理機能分析】` ➔ ⭕ `【二人の性格・コミュニケーションの相性】`
   - ❌ `日干`・`十干`・`地支`・`通変星`・`蔵干` などの単語をそのまま出さず、「あなたの本質を表す星」「守護属性」「生まれ持った才能」等へ噛み砕くこと。

2. **キャラクター口調の統一**:
   - **月（ツキ）**: 優しく包み込み、共感と安心感を与える温かなお姉さん的トーン。
   - **蓮（レン）**: 落ち着きと知性があり、的確かつ背中をそっと押す兄貴分・メンター的トーン。
   - どちらも難解な専門用語を使わず、高校生でも直感的に響く表現を徹底すること。

---

## 3. 📰 コラム記事制作・自動生成レギュレーション

コラム記事（`src/data/columnsData.ts` および `scripts/generate-column.js`）は、以下のレギュレーションを100%遵守して作成・管理すること。

1. **サムネイル画像の完全オリジナル・重複禁止ルール**:
   - コラムの画像は記事ごとに毎回固有の画像URLを設定すること。
   - 過去の記事や既存記事で使われているサムネイル画像URLの使い回し（重複）は**厳格に禁止**。
   - 自動生成スクリプト（`scripts/generate-column.js`）には、既存記事の画像URL重複を検知・自動回避するガード（`Guard 3`）を常時適用すること。

2. **中盤CTAの必須配置**:
   - 記事が長文（2,000字前後）となるため、記事末尾のCTAだけでなく、**記事の中盤（第2章または第3章直後）にも無料相性鑑定へ誘導するCTAバナーを必ず配置**すること。

3. **記事の品質基準**:
   - 文字数：1記事あたり本文最低800文字以上（推奨：1,500〜2,500字）。
   - 構造：目次（TOC）、H2・H3タグ、強調（`<strong>`）、箇条書き（`<ul>` / `<li>`）、FAQ（Q&A 1〜2問）を必ず含むこと。
   - 自動投稿間隔：品質維持およびSEO評価のため、前回の投稿から**最低12時間以上**空けること（`Guard 1`）。

4. **自動更新バッチ（CI/CD）**:
   - GitHub Actions（`.github/workflows/daily_column.yml`）により、毎日午前9時（UTC）に最新コラムが自動生成・静的HTML事前レンダリング・サイトマップ更新され、自動コミット＆プッシュされる構成を維持すること。

---

## 4. 📱 UI / UX・デバイス対応レギュレーション

1. **PWA / スマホ（SP）のセーフエリア（余白）最適化**:
   - iOS Safari および PWAホーム画面追加時の下部バーにおいて、不自然な余白や重なりが生じないよう、`env(safe-area-inset-bottom)` を用いたレイアウトを徹底すること。

2. **鑑定結果のワンタップ保存（UX改善）**:
   - 鑑定結果の共有・保存機能において、「長押し保存」等の難解な操作を強要せず、「画像で保存」ボタンをタップした瞬間に画像生成・即時ダウンロードが行われる仕様を維持すること。

---

## 5. 🔐 認証・インフラ設計レギュレーション

1. **Vercel + Firebase Authentication 構成**:
   - 本番ホスティング: Vercel（カスタムドメイン: `www.tsuki-to-ren.com`）
   - リバースプロキシ: `vercel.json` にて `/__/auth/:path*` ➔ `https://tsuki-to-ren-dba8c.firebaseapp.com/__/auth/:path*` をルーティング。
   - OAuthリダイレクト設定（Google / X）:
     - 承認済みリダイレクトURIに `https://www.tsuki-to-ren.com/__/auth/handler` および `https://tsuki-to-ren-dba8c.firebaseapp.com/__/auth/handler` が必要。
   - メール認証（Passwordless Magic Link）:
     - パスワード不要のメールリンク認証を常時サポートすること。

---

## 6. 💻 開発コマンド・運用フロー

- **開発サーバー起動**:
  ```bash
  npm run dev -- --host 0.0.0.0 --port 5173
  ```
- **ビルド・検証（作業終了前必須）**:
  ```bash
  npm run build
  ```
  ※ `tsc -b`（型チェック）➔ `vite build` ➔ `node scripts/build-sitemap.js` ➔ `node scripts/prerender-columns.js` が自動実行されます。
- **Git コミット＆プッシュ**:
  ```bash
  GIT_CONFIG_GLOBAL=/dev/null git add .
  GIT_CONFIG_GLOBAL=/dev/null git commit -m "fix/feat: commit message"
  GIT_CONFIG_GLOBAL=/dev/null git push origin main
  ```
